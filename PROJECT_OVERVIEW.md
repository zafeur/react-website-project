# مستندات جامع پروژه کی میای

این فایل یک توضیح کامل و قابل‌فهم از ساختار پروژه، صفحات، اجزای اصلی، منطق داده‌ها و تغییراتی است که تا الان روی پروژه انجام شده است. هدف این سند این است که هر کسی بتواند بدون نیاز به بررسی خط‌به‌خط کد، بفهمد پروژه چه کاری انجام می‌دهد، هر بخش چه نقشی دارد و در ادامه توسعه باید به چه نکاتی توجه شود.

---

## معرفی کلی پروژه

پروژه «کی میای» یک وب‌اپلیکیشن فارسی و راست‌به‌چپ است که با `Next.js` و `React` ساخته شده. این سایت برای نمایش کسب‌وکارها، هدایا، تخفیف‌ها، استوری‌ها، صفحات اختصاصی بیزینس‌ها، رستوران ملل، ورود کاربر، کیف پول و داشبورد کاربری استفاده می‌شود.

کاربر می‌تواند در صفحه اصلی برندها و پیشنهادهای فعال را ببیند، وارد صفحه هر کسب‌وکار شود، کد خرید یا تخفیف دریافت کند و در صورت نیاز وارد حساب کاربری شود. پروژه از حالت روشن و تاریک پشتیبانی می‌کند و بخش‌های اصلی آن برای موبایل و دسکتاپ responsive شده‌اند.

تکنولوژی‌های اصلی پروژه:

- `Next.js` برای routing، page rendering و build پروژه
- `React` برای ساخت کامپوننت‌های UI
- `axios` برای ارتباط با API
- `js-cookie` برای نگهداری token ورود کاربر
- `lucide-react` برای آیکن‌ها
- CSS معمولی برای استایل صفحات، حالت تاریک، موبایل و RTL

---

## دستورهای مهم پروژه

```bash
npm run dev
```

اجرای پروژه در حالت توسعه. آدرس معمول پروژه در لوکال:

```txt
http://localhost:3000
```

```bash
npm run build
```

ساخت نسخه production و بررسی اینکه پروژه خطای build نداشته باشد.

```bash
npm run start
```

اجرای نسخه build شده.

نکته: در بعضی شرایط روی ویندوز، اجرای build داخل sandbox ممکن است خطای `spawn EPERM` بدهد. در این حالت build باید خارج از sandbox یا با دسترسی مناسب اجرا شود.

---

## مسیرهای اصلی سایت

### صفحه اصلی: `/`

صفحه اصلی سایت است و کامپوننت `HomePage` را نمایش می‌دهد. این صفحه شامل header، جستجو، استوری‌ها، برندها، بنرها، کارت‌های هدیه و تخفیف، راهنمای پلتفرم و footer است.

داده‌های مهم صفحه اصلی از API گرفته می‌شوند. کارت‌های تخفیف و استوری‌ها در حال حاضر از مسیر `/discount/all` خوانده می‌شوند. اگر API در دسترس نباشد یا داده قابل استفاده ندهد، بعضی بخش‌ها fallback لوکال دارند تا صفحه خالی یا خراب نشود.

### صفحه رستوران ملل: `/restaurant`

صفحه اختصاصی رستوران ملل است. این صفحه اطلاعات رستوران، عکس اصلی، کارت پروفایل، شماره تماس، ساعت کاری، موقعیت مکانی، کیف پول مجموعه، تب‌ها، هدایا، درباره ما، گالری و نظرات کاربران را نمایش می‌دهد.

این صفحه از نظر RTL، حالت تاریک، موبایل و رزولوشن‌های کوچک چندین بار اصلاح شده تا layout به‌هم نریزد و متن‌ها از کارت‌ها بیرون نزنند.

### صفحه کسب‌وکارها: `/business/[business]`

صفحه داینامیک برای کسب‌وکارهاست. مثال‌ها:

```txt
/business/dorato
/business/barial
/business/ibamo
/business/bakhshi
/business/bastani
/business/mojalal
```

این صفحه با توجه به slug داخل URL، اطلاعات همان کسب‌وکار را نمایش می‌دهد. قبلاً بعضی مسیرهای بیزینس 404 می‌شدند؛ مسیر داینامیک اصلاح شد تا صفحات بیزینس‌ها قابل باز شدن باشند.

### داشبورد کاربر: `/dashboard`

صفحه حساب کاربری است. اگر کاربر لاگین نباشد، باید ابتدا وارد شود. بعد از ورود، اطلاعات مربوط به حساب، کیف پول، امتیازها و بخش‌های کاربری نمایش داده می‌شوند.

### کیف پول کاربر: `/dashboard/wallet`

صفحه کیف پول کاربر است و برای استفاده از آن کاربر باید لاگین کرده باشد.

### سوالات متداول: `/faq`

صفحه سوالات متداول و راهنمای عضویت یا استفاده از پلتفرم است.

---

## منطق ورود و خروج کاربر

ورود کاربر با شماره موبایل و OTP انجام می‌شود. روند کلی:

1. کاربر شماره موبایل وارد می‌کند.
2. درخواست ارسال OTP به API فرستاده می‌شود.
3. کاربر کد OTP را وارد می‌کند.
4. در صورت تایید، token ذخیره می‌شود.
5. بخش‌های نیازمند ورود برای کاربر فعال می‌شوند.

منطق خروج هم اصلاح شده است. وقتی کاربر logout می‌کند:

- token حذف می‌شود.
- وضعیت لاگین پاک می‌شود.
- کاربر به صفحه اصلی `/` هدایت می‌شود.
- اگر بعداً بخواهد از خدمات نیازمند ورود استفاده کند، دوباره modal ورود نمایش داده می‌شود.

این رفتار مهم است، چون کاربر نباید بعد از خروج داخل صفحه رستوران یا صفحه‌ای با کادر لاگین گیر کند.

---

## منطق API و داده‌ها

### API اصلی پروژه

Base URL پروژه از فایل `.env` خوانده می‌شود:

```txt
NEXT_PUBLIC_API_BASE_URL=https://api.didarads.com/api/v1
VITE_API_BASE_URL=https://api.didarads.com/api/v1
```

درخواست‌های عمومی و auth از طریق `httpClient` مدیریت می‌شوند تا headerها و token در یک مسیر مشترک کنترل شوند.

### کارت‌های تخفیف و هدیه

کارت‌های تخفیف از مسیر زیر خوانده می‌شوند:

```txt
/discount/all
```

این API داده کارت‌ها را برمی‌گرداند. کارت‌ها بر اساس داده API نمایش داده می‌شوند و اگر API کارت بدهد، همان کارت‌ها اولویت دارند.

کارت‌ها از نظر منطق داخلی سه حالت دارند:

1. کارت VIP: هم هدیه دارد و هم تخفیف دارد و کلید VIP آن فعال است.
2. کارت غیر VIP با هدیه و تخفیف: هدیه و تخفیف دارد، اما VIP نیست.
3. کارت تخفیف ساده: فقط تخفیف دارد.

نکته مهم: این سه حالت برای منطق داخلی استفاده می‌شوند، اما کاربر نباید کارت‌ها را در چند بخش جدا ببیند. همه کارت‌ها در یک بخش مشترک نمایش داده می‌شوند. فقط کارت‌های VIP باید از ظاهرشان واضح و قابل تشخیص باشند.

### تشخیص کارت VIP

تشخیص VIP بودن از سمت بک‌اند/API مشخص می‌شود. یعنی بک‌اند باید داخل داده هر کارت مقدار VIP را ارسال کند، مثلاً:

```json
"vip": 1
```

اگر مقدار VIP فعال باشد، فرانت‌اند ظاهر کارت را VIP نمایش می‌دهد. اگر مقدار `vip` برابر صفر باشد یا هیچ کلید VIP معتبری ارسال نشود، کارت معمولی نمایش داده می‌شود.

فرانت‌اند چند نام مختلف برای کلید VIP را هم پشتیبانی می‌کند، مثل:

```txt
vip
is_vip
isVip
isVIP
vip_flag
vipStatus
vip_discount
is_vip_discount
```

اما تصمیم اصلی VIP بودن با بک‌اند است. فرانت‌اند فقط مقدار API را می‌خواند و ظاهر را تغییر می‌دهد.

### ظاهر کارت‌های VIP

برای اینکه کارت‌های VIP واضح باشند، طراحی آن‌ها متفاوت شده است:

- برچسب مشخص `کارت وی‌آی‌پی`
- کادر و glow طلایی/سبز
- chip وی‌آی‌پی کنار عنوان
- دکمه متفاوت با متن `دریافت کد VIP`
- ظاهر برجسته‌تر نسبت به کارت‌های معمولی

اگر در صفحه کارت VIP دیده نمی‌شود، معمولاً یعنی API در حال حاضر هیچ کارتی با `vip: 1` ارسال نکرده است.

---

## منطق استوری‌ها

استوری‌ها قبلاً از داده لوکال استفاده می‌کردند یا اگر API ناقص بود، ویدیوهای لوکال به صورت خودکار جایگزین می‌شدند. این منطق اصلاح شده است.

رفتار فعلی:

1. اولویت اول استوری‌ها از API مسیر `/discount/all` و بخش `story.data` است.
2. اگر آن بخش وجود نداشته باشد، کد هنوز می‌تواند storyهای احتمالی `/home` را هم بخواند.
3. اگر هیچ استوری قابل استفاده‌ای از API نیاید، استوری‌های لوکال به عنوان fallback نمایش داده می‌شوند.
4. وقتی استوری از API می‌آید، تصویر و ویدیوی همان API استفاده می‌شود و ویدیوی لوکال به صورت مخفیانه جایگزین نمی‌شود.

لینک‌های API استوری که بررسی و match شده‌اند:

| کسب‌وکار | ویدیوی API | تصویر API |
|---|---|---|
| باستانی | `https://api.keymiay.com/story/bastani.mp4` | `https://api.keymiay.com/images/logo bastani.jpg` |
| مجلل | `https://api.keymiay.com/story/mojallal.mp4` | `https://api.keymiay.com/images/mojalal.jpg` |
| ایبامو | `https://api.keymiay.com/story/ibamo.mp4` | `https://api.keymiay.com/images/logo ibamo.jpg` |
| باریال | `https://api.keymiay.com/story/barial.mp4` | `https://api.keymiay.com/images/barial.jpg` |
| دوراتو | `https://api.keymiay.com/story/dorato.mp4` | `https://api.keymiay.com/images/logo dorato.jpg` |
| بخشی | `https://api.keymiay.com/story/bakhshi.mp4` | `https://api.keymiay.com/images/logo bakhshi.jpeg` |

نکته مهم برای مجلل: لینک درست API این است:

```txt
https://api.keymiay.com/story/mojallal.mp4
```

نه این:

```txt
https://api.keymiay.com/story/mojalal.mp4
```

برای همین alias مربوط به `mojallal` هم به منطق تشخیص مجلل اضافه شده است.

استوری‌های لوکال هنوز به عنوان fallback نگه داشته شده‌اند:

```txt
/home/videos/barial.mp4
/home/videos/bastani.mp4
/home/videos/dorato.mp4
/home/videos/ibamo.mp4
/home/videos/mojalal.mp4
/home/videos/bakhshi.mp4
```

اما این‌ها فقط وقتی استفاده می‌شوند که API استوری قابل استفاده ندهد.

---

## ساختار فایل‌ها و نقش هر بخش

### فایل‌های Next.js

#### `pages/_app.jsx`

ورودی اصلی همه صفحات Next.js است. فایل‌های CSS سراسری و wrapperهای اصلی از اینجا وارد می‌شوند.

#### `pages/_document.jsx`

برای تنظیمات سطح HTML document استفاده می‌شود؛ مثل ساختار اولیه document، زبان و موارد پایه صفحه.

#### `pages/index.jsx`

مسیر صفحه اصلی `/` را به کامپوننت `HomePage` وصل می‌کند.

#### `pages/restaurant.jsx`

مسیر `/restaurant` را نمایش می‌دهد و صفحه رستوران ملل را از طریق ساختار اصلی پروژه render می‌کند.

#### `pages/business/[business].jsx`

مسیر داینامیک صفحات کسب‌وکارهاست. این فایل باعث می‌شود URLهایی مثل `/business/dorato` یا `/business/barial` بدون 404 باز شوند.

#### `pages/dashboard.jsx`

صفحه داشبورد کاربر است.

#### `pages/dashboard/wallet.jsx`

صفحه کیف پول کاربر است.

#### `pages/faq.jsx`

صفحه سوالات متداول است.

#### `middleware.js`

برای middleware/proxy پروژه استفاده می‌شود. در build فعلی Next.js هشدار می‌دهد که convention قدیمی `middleware` بهتر است در آینده به `proxy` منتقل شود.

---

## کامپوننت‌های اصلی

### `src/App.jsx`

کامپوننت مرکزی برای بعضی صفحات داخلی پروژه است. این فایل وضعیت کلی اپلیکیشن، ورود/خروج کاربر، modal ورود، OTP، dashboard، صفحه رستوران و navigation داخلی را مدیریت می‌کند.

### `src/components/HomePage.jsx`

مهم‌ترین کامپوننت صفحه اصلی است. مسئولیت‌های اصلی:

- گرفتن داده صفحه اصلی
- گرفتن کارت‌های تخفیف از `/discount/all`
- گرفتن استوری‌ها از `story.data` داخل پاسخ API
- نمایش برندها، بنرها، استوری‌ها، کارت‌های تخفیف و راهنمای پلتفرم
- کنترل اسلایدر بنر
- کنترل popup استوری
- کنترل ورود از صفحه اصلی
- درخواست کد خرید/تخفیف
- تشخیص VIP بودن کارت‌ها بر اساس API
- fallback به داده لوکال فقط وقتی API داده مناسب ندارد

### `src/components/HomePage.css`

استایل کامل صفحه اصلی است. شامل:

- layout صفحه اصلی
- header صفحه اصلی
- استوری‌ها
- بنرها
- کارت‌های برند
- کارت‌های تخفیف
- ظاهر VIP
- حالت تاریک
- responsive موبایل
- scrollbar و hoverها

در این فایل چند بار cleanup انجام شده تا قوانین تکراری و overrideهای قدیمی کمتر شوند، مخصوصاً برای دکمه‌های کارت‌های تخفیف.

### `src/components/RestaurantPage.jsx`

صفحه رستوران ملل را می‌سازد. شامل کارت پروفایل، تصویر اصلی، اطلاعات تماس، ساعت کاری، موقعیت، کیف پول، تب‌ها، هدایا، درباره ما، گالری و نظرات کاربران.

### `src/components/RestaurantPage.css`

استایل صفحه رستوران ملل است. بیشترین تمرکز اصلاحات این فایل روی RTL، حالت تاریک، موبایل، اندازه کارت‌ها، جلوگیری از بیرون‌زدن متن و تصویر، و هماهنگی layout با طرح مرجع بوده است.

### `src/components/BusinessProfilePage.jsx`

صفحه پروفایل کسب‌وکارها را می‌سازد. بر اساس slug کسب‌وکار، اطلاعات همان برند را نمایش می‌دهد.

### `src/components/BusinessProfilePage.css`

استایل صفحه پروفایل کسب‌وکارهاست؛ شامل کارت پروفایل، تصویر اصلی، کارت‌های اطلاعاتی، کیف پول اختصاصی مجموعه، دکمه‌ها، حالت تاریک و responsive.

### `src/components/DashboardPage.jsx`

داشبورد کاربر را نمایش می‌دهد؛ شامل اطلاعات حساب، کیف پول، هدایا و وضعیت کاربر.

### `src/components/DashboardPage.css`

استایل داشبورد کاربر است.

### `src/components/FaqMembershipPage.jsx`

صفحه سوالات متداول و راهنمای عضویت را نمایش می‌دهد.

### `src/components/FaqMembershipPage.css`

استایل صفحه FAQ و بخش راهنماست.

### `src/components/Header.jsx`

header مشترک صفحات داخلی است. جای لوگو، navigation، دکمه ورود و toggle حالت تاریک در این بخش مدیریت می‌شود. چیدمان header برای RTL و شباهت به صفحه اصلی اصلاح شده است.

### `src/components/Header.css`

استایل header مشترک پروژه است.

### `src/components/LoginModal.jsx`

modal ورود و ثبت‌نام است. ورود با شماره موبایل و OTP از اینجا انجام می‌شود.

### `src/components/LoginModal.css`

استایل فرم ورود و OTP است.

### `src/components/MobileBottomNav.jsx`

نوار پایین موبایل است و لینک‌های اصلی مثل صفحه اصلی، فروشگاهی، هدایا، سوالات و ورود/حساب را نمایش می‌دهد.

### `src/components/MobileBottomNav.css`

استایل bottom navigation موبایل است.

### `src/components/GlobalDarkMode.css`

قوانین عمومی حالت تاریک را نگه می‌دارد تا پس‌زمینه‌ها، متن‌ها، کارت‌ها و رنگ‌ها در dark mode خوانا باشند.

---

## فایل‌های API

### `src/api/auth.js`

درخواست‌های ورود را مدیریت می‌کند:

- ارسال OTP
- تایید OTP

### `src/api/home.js`

درخواست‌های صفحه اصلی و تخفیف‌ها را مدیریت می‌کند:

- `getHomePageData()` برای `/home`
- `getDiscountCards()` برای `/discount/all`
- `requestDiscountCode()` برای گرفتن یا درخواست کد خرید/تخفیف

در عمل، `/discount/all` در حال حاضر منبع مهم کارت‌های تخفیف و استوری‌هاست.

### `src/api/wallet.js`

درخواست‌های مربوط به کیف پول کاربر و کیف پول کسب‌وکارها را مدیریت می‌کند. این بخش برای جلوگیری از 404 در صفحه‌های کیف پول بیزینس‌ها مهم است.

### `src/api/membership.js`

درخواست‌های مربوط به عضویت یا membership را مدیریت می‌کند.

---

## فایل‌های helper

### `src/helper/httpClient.js`

یک instance مشترک از axios می‌سازد و base URL پروژه را تنظیم می‌کند. بهتر است درخواست‌های API عمومی از همین مسیر انجام شوند تا تنظیمات مشترک حفظ شود.

### `src/helper/authMiddleware.js`

قبل از ارسال درخواست، وضعیت auth را بررسی می‌کند. اگر درخواست نیاز به ورود داشته باشد و token وجود نداشته باشد، درخواست رد می‌شود. اگر token موجود باشد، در header با فرمت `Bearer token` ارسال می‌شود.

### `src/helper/authCookie.js`

وظیفه ذخیره، خواندن و حذف token را دارد. همچنین token و user type را از پاسخ login استخراج می‌کند.

---

## داده‌های ثابت و فایل‌های public

### `src/data/siteData.js`

داده‌های ثابت پروژه را نگه می‌دارد؛ مثل تب‌های رستوران، کارت‌های اطلاعاتی، هدایا، گالری، پروفایل کسب‌وکارها، لینک نقشه، اینستاگرام و داده‌های نمونه کیف پول.

### `public/home/img`

تصاویر عمومی صفحه اصلی، لوگوها و عکس‌های برندها در این مسیر قرار دارند.

### `public/home/img/banner`

بنرهای صفحه اصلی برای برندهای مختلف.

### `public/home/img/business-banners`

تصاویر hero و بنرهای اختصاصی کسب‌وکارها.

### `public/home/videos`

ویدیوهای استوری لوکال. این فایل‌ها fallback هستند و وقتی API استوری قابل استفاده بدهد، نباید جایگزین ویدیوی API شوند.

### `public/home/fonts`

فونت‌های Vazirmatn برای نمایش بهتر متن فارسی.

---

## تغییرات مهم انجام‌شده تا الان

### اصلاح صفحه رستوران ملل

- چیدمان کلی صفحه به طرح مرجع نزدیک شد.
- کارت پروفایل، تصویر اصلی و کارت‌های اطلاعاتی مرتب شدند.
- کارت‌های تماس، ساعت کاری و موقعیت هم‌ارتفاع و خواناتر شدند.
- متن موقعیت مکانی از کارت بیرون نمی‌زند.
- گالری درباره ما در رزولوشن‌های کوچک از کادر خارج نمی‌شود.
- tabs در موبایل و دسکتاپ بهتر کنترل شدند.

### اصلاح header و RTL

- لوگوی سایت باید در سمت راست باقی بماند.
- دکمه ورود و toggle حالت تاریک جای درست‌تری نسبت به هم پیدا کردند.
- header صفحات داخلی با صفحه اصلی هماهنگ‌تر شد.
- در بعضی رزولوشن‌ها که آیتم‌ها جابه‌جا یا مخفی می‌شدند، چیدمان اصلاح شد.

### اصلاح dark mode

- متن‌هایی که در حالت تاریک دیده نمی‌شدند خوانا شدند.
- رنگ کارت‌ها، borderها، دکمه‌ها و scrollbarها با dark mode هماهنگ‌تر شدند.
- hover دکمه‌ها در light mode و dark mode قابل خواندن باقی می‌ماند.

### اصلاح responsive موبایل

- horizontal scrollهای نامناسب کاهش پیدا کردند.
- بخش‌هایی مثل tabs، گالری و کارت‌های اطلاعاتی در موبایل مرتب‌تر شدند.
- فاصله‌های خالی و بیرون‌زدگی عناصر در رزولوشن‌های کوچک اصلاح شد.

### اصلاح مسیرهای 404

- صفحات داینامیک بیزینس‌ها مثل `/business/bastani` اصلاح شدند.
- مشکل 404 بعضی صفحات کیف پول کسب‌وکارها بررسی و اصلاح شد.

### اضافه شدن راهنمای پلتفرم

در صفحه اصلی بخشی برای راهنمای استفاده از پلتفرم اضافه شد. مراحل کلی:

1. ورود با شماره تماس
2. کلیک روی دریافت کد تخفیف
3. دریافت کد تخفیف
4. مشاهده امتیاز و جوایز از پروفایل کاربری

### اصلاح کارت‌های تخفیف

- کارت‌ها از API `/discount/all` خوانده می‌شوند.
- کارت‌ها در یک بخش مشترک نمایش داده می‌شوند، نه چند بخش جدا.
- منطق VIP، هدیه + تخفیف، و تخفیف ساده در کد حفظ شده است.
- کارت VIP ظاهر متفاوت و مشخص دارد.
- توضیحات کارت‌ها با کسب‌وکار مربوطه هماهنگ‌تر شده‌اند.
- کارت‌های fallback تکراری با کارت‌های API ادغام می‌شوند تا duplicate ایجاد نشود.

### اصلاح استوری‌ها

- استوری‌ها از `story.data` داخل پاسخ `/discount/all` خوانده می‌شوند.
- اگر API استوری بدهد، همان ویدیو و تصویر API استفاده می‌شود.
- اگر API استوری ندهد یا شکست بخورد، استوری‌های لوکال fallback هستند.
- لینک درست مجلل `mojallal.mp4` است و alias آن اضافه شده است.

### حذف یا فیلتر بخش‌های غیرمرتبط

- دسته‌بندی‌های مربوط به pet یا موارد غیرمرتبط از نمایش صفحه اصلی فیلتر شدند.

### ساخت `.gitignore`

فایل `.gitignore` برای جلوگیری از ورود فایل‌های غیرضروری مثل `node_modules`، خروجی build و فایل‌های محیطی ساخته شد.

---

## نکات مهم برای ادامه توسعه

- منطق کلی پروژه بدون نیاز تغییر نکند.
- اگر مشکل فقط ظاهری است، تغییر باید تا حد ممکن محدود به CSS همان بخش باشد.
- داده‌های API باید منبع اصلی باشند و داده لوکال فقط fallback باشد.
- برای کارت VIP، بک‌اند باید مقدار VIP را مشخص کند؛ فرانت‌اند فقط آن را نمایش می‌دهد.
- برای استوری‌ها، اگر API ویدیو بدهد، همان ویدیوی API استفاده شود.
- برای درخواست‌های نیازمند ورود از `requiresAuth: true` استفاده شود.
- درخواست‌های عمومی مثل کارت‌های تخفیف و استوری‌ها نباید بی‌دلیل وابسته به login باشند.
- بعد از تغییرات مهم، `npm run build` اجرا شود.
- در RTL همیشه جای واقعی start/end بررسی شود، چون left/right ممکن است باعث نتیجه برعکس شود.
- dark mode باید از نظر خوانایی متن، دکمه و scrollbar بررسی شود.
- در موبایل، تا جای ممکن از horizontal scroll برای بخش‌های اصلی جلوگیری شود.

---

## فایل‌هایی که بیشتر تغییر کرده‌اند

- `src/components/HomePage.jsx`
- `src/components/HomePage.css`
- `src/components/RestaurantPage.jsx`
- `src/components/RestaurantPage.css`
- `src/components/BusinessProfilePage.jsx`
- `src/components/BusinessProfilePage.css`
- `src/components/Header.jsx`
- `src/App.jsx`
- `src/api/home.js`
- `src/api/wallet.js`
- `src/data/siteData.js`

---

## وضعیت فعلی build

آخرین build بعد از تغییرات مربوط به استوری‌های API و fallback لوکال موفق بوده است:

```bash
npm run build
```

اگر در محیط ویندوز خطای `spawn EPERM` دیده شد، مشکل معمولاً از sandbox اجرای build است، نه لزوماً از کد پروژه.
